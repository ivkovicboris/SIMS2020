<Window x:Class="SIMS2020.FRONT.ErrorMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SIMS2020.FRONT"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Title="Error">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0"  Text="{Binding Message}" MinWidth="300"/>
        <WrapPanel Grid.Row="1" HorizontalAlignment="Right" Margin="0,15,5,5">
            <Button  IsDefault="True" MinWidth="60" Margin="0,0,10,0" Content="OK" Click="OKButton_Click"/>
        </WrapPanel>
    </Grid>
</Window>
